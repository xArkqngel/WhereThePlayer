export interface GameCoverType {
    name: string;
    url: string;
    id: string;
  }

export interface MapType {
    name: string;
    image: string;
}

export interface MapImage{
  id: string;
  image_path: string;
  map_name: string;
}